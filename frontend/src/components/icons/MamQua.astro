---
interface Props { size?: number; class?: string }
const { size = 24, class: className } = Astro.props;
---
<!-- Mâm Quả — mâm ngũ quả truyền thống trong lễ ăn hỏi, cưới hỏi -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width={size} height={size}
  fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"
  class={className} aria-hidden="true">
  <!-- 3 quả tươi xếp trên mâm -->
  <circle cx="9.5" cy="11" r="2.5"/>
  <circle cx="14.5" cy="11" r="2.5"/>
  <circle cx="12" cy="8" r="2.5"/>
  <!-- Mâm đĩa -->
  <ellipse cx="12" cy="14" rx="8" ry="2"/>
  <!-- Chân đế -->
  <line x1="12" y1="15.5" x2="12" y2="17.5"/>
  <!-- Đế mâm -->
  <ellipse cx="12" cy="18.5" rx="5" ry="1.5"/>
</svg>
